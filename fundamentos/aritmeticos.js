const [a, b, c, d] = [3, 5, 1, 15] // forma reduzida de criar 4 variáveis (usando destructuring)

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)

// binário: nmr1 + nmr2 (dois operandos)
// unário: -nmr1 (um operando)