const raio = 5.6
const area = Math.PI * Math.pow(raio, 2)

console.log((area).toFixed(2))

console.log(typeof Math) // object

const quadrado = Math.pow(10, 2) // pow = potenciação
console.log(quadrado)